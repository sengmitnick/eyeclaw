require 'rails_helper'

RSpec.describe BindingToken, type: :model do
  # Example: Basic factory test
  # it 'can be created with factory' do
  #   record = create(:binding_token)
  #   expect(record).to be_persisted
  # end
end
