require 'rails_helper'

RSpec.describe StreamTrace, type: :model do
  # Example: Basic factory test
  # it 'can be created with factory' do
  #   record = create(:stream_trace)
  #   expect(record).to be_persisted
  # end
end
